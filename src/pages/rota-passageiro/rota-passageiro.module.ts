import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { RotaPassageiroPage } from './rota-passageiro';

@NgModule({
  declarations: [
    RotaPassageiroPage,
  ],
  imports: [
    IonicPageModule.forChild(RotaPassageiroPage),
  ],
})
export class RotaPassageiroPageModule {}
